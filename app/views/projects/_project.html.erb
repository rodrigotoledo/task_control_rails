<%= turbo_stream_from project %>
<div id="<%= dom_id project %>">
  <p class="my-5">
    <strong class="block font-medium mb-1">Title:</strong>
    <%= project.title %>
  </p>

  <p class="my-5">
    <% if project.feature_image.attached? %>
      <%= image_tag project.feature_image.variant(resize_to_limit: [100, 100]) %>
    <% else %>
      <p>No image available</p>
    <% end %>
  </p>


  <p class="my-5">
    <strong class="block font-medium mb-1">Completed at:</strong>
    <%= l(project.completed_at, format: :long) unless project.completed_at.blank? %>
  </p>
  
  <%= link_to "Edit this project", edit_project_path(project), class: "rounded-lg py-3 ml-2 px-5 bg-gray-100 inline-block font-medium" %>
  <div class="inline-block ml-2">
    <%= button_to "Destroy this project", project_path(project), method: :delete, class: "mt-2 rounded-lg py-3 px-5 bg-red-800 text-white font-medium", form: { data: { turbo_confirm: 'Are you sure?' } } %>
  </div>
  <hr class="mt-6">
</div>
